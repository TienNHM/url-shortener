export const AppConsts = {
    PROJECT_DIR : __dirname,
    ENV : process.env.NODE_ENV || 'development',
    DEVELOPMENT: 'development',
    PRODUCTION: 'production',
};

export const DB_SLAVE_CONNECTION = 'mysqlSlaveConnection';
